
# Workspace directory for cloning repos (local dev)
WORKTREE_DIR=/tmp/ai-bug-fixer-workspaces

# Enable Docker socket mount in agent containers
# Required for tools like Supabase CLI that spin up containers
ENABLE_DOCKER_IN_AGENT=false
